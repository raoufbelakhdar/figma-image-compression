{
  "name": "figma-image-compression",
  "version": "1.0.0",
  "description": "Platform for compressing images for Figma using webpack.",
  "scripts": {
    "start": "webpack serve --config webpack.config.js --mode development",
    "build": "webpack --config webpack.config.js --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Timur_etc",
  "license": "MIT",
  "devDependencies": {
    "html-webpack-plugin": "^1.4.0",
    "webpack": "^5.22.0",
    "webpack-cli": "^4.3.0",
    "ts-loader": "^8.0.0",
    "style-loader": "^3.3.0",
    "css-loader": "^3.6.0"
  }
}